import React from "react";
class Failed extends React.Component {
  componentDidMount() {
  }

  render() {
    return <>Payment Failed, Go back to <a href="#">review</a> page to try again</>;
  }
}
export default Failed;